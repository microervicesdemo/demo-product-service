--drop table t_product;

create table t_product (ID INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY, value integer,
                        NAME varchar(50) not null, details varchar(50) );
                        
